brew install postgresql
brew services start postgresql
psql postgres
CREATE DATABASE namechart;
CREATE USER namechart_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE namechart TO namechart_user;
psql -U namechart_user -d namechart -h localhost -p 5432
python manage.py migrate
python manage.py shell
from django.db import connection
cursor = connection.cursor()
cursor.execute("SELECT 1;")
print(cursor.fetchone())
    If you see `(1,)` as the output, the connection is working correctly.
