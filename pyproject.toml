[project]
name = "namechart"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0",
    "ged4py>=0.5.2",
    "wand>=0.6.13",
]

[dependency-groups]
dev = [
    "django-debug-toolbar>=6.1.0",
]
